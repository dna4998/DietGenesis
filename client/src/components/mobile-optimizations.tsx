// Mobile optimizations are now handled automatically through responsive design
// This component has been removed - the app uses useIsMobile hook for device detection

export default function MobileOptimizations() {
  return null;
}